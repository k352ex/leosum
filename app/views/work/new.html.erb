<%= form_for @work, url: {action: "create"} do |w| %>
  <p>
    <%= w.label "타이틀" %>
    <%= w.text_field :title %>
    <%= hidden_field_tag(:user_id, current_user.id) %>
  </p>

  <%= w.submit "제출" %>
<% end %>
