  <!-- 전제 넓이는 10 -->
  <div class="row">
    <div class="col-xs-12 col-md-10 col-md-offset-1">
      <!-- 썸네일 표시 -->
      <% @post.each do |p| %>
        <div class="col-xs-3 col-md-1">
          <a href="/home/detail/<%= p.id %>" class="thumbnail"> <%= image_tag("#{p.image}", alt: " ") %> </a>
        </div>
      <% end %>
    </div>
  </div>
